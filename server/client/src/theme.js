import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#820600',
    },
    secondary: {
      main: '#AB5D00',
    },
    // mode: 'dark',

  },
  spacing: 8,
});

export default theme;